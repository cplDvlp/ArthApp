Para compilar el proyecto se necesitan 2 terminales

En la primera -> truffle develop
Y despues de que se ejecute -> migrate

En la siguiente terminal, se accede a la carpeta de vue
cd arthApp
npm run serve

Enlace para acceder a la aplicación -> http://localhost:8080/
Network de metamask para las pruebas de ganache -> localhost 9545